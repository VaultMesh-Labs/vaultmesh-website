name: nav-footer-guard

on:
  push:
  pull_request:

jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run nav/footer drift guard
        run: bash scripts/nav_footer_guard.sh
