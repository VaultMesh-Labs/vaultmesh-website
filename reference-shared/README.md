# Reference Shared Skin (Frozen)

This directory contains a frozen reference skin artifact (`ui.css`) for comparison only.

Do not deploy from this path.
Authoritative deploy source is `public/shared/ui.css`.
